/* global DCLib */

// (function(){
window.MyDApp_debug = (function () {
  var myDApp = new DCLib.DApp({slug: 'dicetest_v32'})

  // Banroller side code
  // console.log(myDApp)

  return myDApp
})()
